import tkinter as tk
window = tk.Tk()
greeting = tk.Label(text="Hello, Heet kumar")
greeting.pack()  #to add widgets to a window, you can use the label widgets  .pack() method:
window.mainloop()
#when you .pack() a widget into a window, Tkinter sizes the window as small as
#it can while still fully encompassing the widgets
# code to add widgets will go here....

# practical exam : module 2 and module 3